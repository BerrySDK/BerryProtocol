{
  "name": "berrysdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Berry SDK monorepo for WhatsApp Web SDK, transport, proto wrappers, and publishing packages.",
  "author": "Lipe Devv and Berry Protocol",
  "license": "Apache-2.0",
  "keywords": [
    "whatsapp",
    "baileys",
    "sdk",
    "bot",
    "berrysdk",
    "whatsapp-web"
  ],
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "baileys": "7.0.0-rc10",
    "qrcode-terminal": "^0.12.0"
  },
  "scripts": {
    "postinstall": "node scripts/patch-baileys-esm.mjs",
    "build": "tsc -b packages/events packages/protocol packages/messages packages/wa-message packages/media packages/store packages/auth packages/socket packages/core packages/gateway packages/cli",
    "clean": "powershell -NoProfile -Command \"Get-ChildItem packages -Directory | ForEach-Object { Remove-Item -Recurse -Force (Join-Path $_.FullName 'dist') -ErrorAction SilentlyContinue; Remove-Item -Force (Join-Path $_.FullName '.tsbuildinfo') -ErrorAction SilentlyContinue }\"",
    "prepare:publish": "npm run clean && npm run build",
    "pack:check": "npm pack --workspaces --json",
    "dev:gateway": "npm run build && node packages/gateway/dist/index.js",
    "dev:cli": "npm run build && node packages/cli/dist/index.js",
    "start:gateway": "node packages/gateway/dist/index.js",
    "start:cli": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.3",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/ws": "^8.18.1",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
